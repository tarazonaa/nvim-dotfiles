require('Comment').setup{
    padding = true,
    sticky = true,

    opleader = {
        line = 'gc',
        block = 'gb',
    },
    mappings = {
        basic = true,
        extra = true,
    },
}
